package com.kelway.cosmoalias.presentation

import com.kelway.cosmoalias.R

enum class StatusTeam(val action: Int) {
    AWAITING(R.string.awaiting_status),
    PLAYS(R.string.plays_status),
    PLAYED(R.string.played_status)
}